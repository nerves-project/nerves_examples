import Config

# In most cases, what you would consider "dev" in phoenix should go in the
# host.exs. However, if you have dev specific configuration shared between
# host and target, it should go here.
